/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: "https",
        hostname: "img.clerk.com",
      },
    ],
    domains: ["i.postimg.cc"],
  },
};

module.exports = nextConfig;
